extends layout

block content
  .container
    .content
      h1= title
      hr
      br
      if error
        .alert.alert-danger #{error}
      else
        table.table.table-striped.table-bordered.table-hover
          if inventory
            thead
              tr
                th ID
                th First Name
                th Last Name
                th EMail
            tbody
              for contact in inventory
                tr
                  td= contact.id
                  td= contact.firstname
                  td= contact.lastname
                  td= contact.email
          else
            form(action="/seed", method="POST")
                button(type="submit").btn.btn-primary Seed Data
            br
            br
            tr
              td
                | No inventory found
